{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'title.edit_board'|trans({name: board.name}) }}</h1>

    <div>
        {{ include('App/Board/_form.html.twig') }}
    </div>
{% endblock %}