{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'title.add_image'|trans({board: board.name}) }}</h1>

    <div>
        {{ include('App/Image/_form.html.twig') }}
    </div>
{% endblock %}